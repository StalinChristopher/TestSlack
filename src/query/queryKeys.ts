export const QueryKeys = {
  // Define your query keys here, for example:
  // users: {
  //   all: () => ['users'] as const,
  //   detail: (id: string) => ['users', id] as const,
  // },
} as const;
